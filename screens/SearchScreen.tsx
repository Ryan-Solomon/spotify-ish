import * as React from 'react';
import styled from 'styled-components/native';

export default function TabTwoScreen() {
  return (
    <SContainer>
      <SText>Search Screen</SText>
    </SContainer>
  );
}

const SContainer = styled.View``;

const SText = styled.Text`
  color: white;
`;
